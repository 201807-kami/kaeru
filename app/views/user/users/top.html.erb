<% flash.each do |message_type, message| %>
<div class="alert alert-<%= message_type %>"><%= message %></div>
<% end %>

  &nbsp;&nbsp;&nbsp;&nbsp;<div class="swiper-container">
            <div class="swiper-wrapper">
              <% @items.each do |item| %>
            <div class="swiper-slide"><%= attachment_image_tag item, :item_image, :fill, 500, 300 %></div>
 <!--              <div class="swiper-slide"><%= item.item_image %></div>
              <div class="swiper-slide"><%= item.item_image %></div> -->
            <!-- </div> -->
          <% end %>
          </div>
        </div>


<h4> ランキング </h4>
<table class="table">
        <thead>
            <tr>
                <th>１位</th>
                <th>２位</th>
                <th>３位</th>
                <th>４位</th>
                <th>５位</th>
            </tr>
        </thead>

        <tbody>
           <% @rank_items.each do |rank| %>
           <th>
              <%= attachment_image_tag rank, :item_image, :fill, 130, 130, format: 'jpeg', class: "profile-img center-block" %>
              <p><%= link_to rank.title, user_item_path(rank.id) %></p>
              <p><%= rank.artist.name %></p>
              <% end %>
          </th>
        </tbody>
    </table>


<div class="text-center">

<h4 class="page-header">ネットストアの便利なサービス内容</h4>
  <div class="col-lg-4">
    <p>
      <i class="fa fa-truck fa-5x"></i>
       <br clear="left">
       配送料無料
    </p>
  </div>
  <div class="col-lg-4">
    <p>
     <i class="fa fa-archive fa-5x"></i>
       <br clear="left">
       選べる受取方法
    </p>
   </div>
   <div class="col-lg-4">
   	<p>
     <i class="fa fa-shopping-bag fa-5x"></i>
      <br class="clear">
      まとめ買いOK
    </p>
   </div>


</div>



